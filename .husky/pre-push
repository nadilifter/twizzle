if [ "${SKIP_LINT:-}" = "1" ]; then
  echo "SKIP_LINT=1, skipping lint check."
  exit 0
fi

echo "Running full lint check before push..."
pnpm run lint

echo "Regenerating Prisma client types..."
pnpm exec prisma generate

echo "Running type check before push..."
pnpm typecheck
