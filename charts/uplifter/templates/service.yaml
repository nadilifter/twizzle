apiVersion: v1
kind: Service
metadata:
  name: {{ include "uplifter.fullname" . }}
  labels:
    {{- include "uplifter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uplifter.selectorLabels" . | nindent 4 }}
